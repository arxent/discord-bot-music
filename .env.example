# === Discord ===
# Token bot dari Portal Discord Developer (Bot > Token)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Opsional: jika ffmpeg tidak ada di PATH, isi path lengkapnya (contoh Windows)
# Di Windows: pastikan pakai double backslash atau bungkus dengan kutip
# FFMPEG_PATH="C:\\ffmpeg\\bin\\ffmpeg.exe"
# Di macOS/Linux: biasanya cukup "ffmpeg" jika sudah terpasang via brew/apt
FFMPEG_PATH=ffmpeg

# Opsional: GUILD_ID hanya jika kamu ingin behavior khusus satu guild.
# Kode kamu saat ini sync global + per-guild otomatis, jadi ini boleh kosong.
# GUILD_ID=123456789012345678


# === Spotify (opsional, untuk perintah /spotify) ===
# Client ID & Secret dari Dashboard Spotify Developer (App > Settings)
# Jika tidak diisi, fitur /spotify akan nonaktif & fallback hanya ke YouTube.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
